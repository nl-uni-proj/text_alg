Functions are prevalent in rust code. 
You have already seen one of the most important functions in the language: 
the main function, which is the entry point of many programs. 
You have also seen the `fn` keyword, which allows you to declare new functions.
